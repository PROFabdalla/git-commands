task githup from abdalla mohamed abdalla amer
track python

git init
git config --global user.name "PROFabdalla"
git config --global user.email "abdallaamer101@gmail.com"
git branch -M main
## git config --global init.defaultBranch main
git config pull.rebase false
git status
git add .
git commit -m "cleating file and write my steps in"

ssh-keygen -t rsa -b 4096 -C "abdallaamer101@gmail.com"

git remote add origin git@github.com:PROFabdalla/abdalla-mohamed.git ---> ssh key

git push origin main
